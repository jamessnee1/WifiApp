<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="group107.wifiapp.SettingsActivity"
    android:background="#282828">


    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wifi on/off"
        android:id="@+id/settingsWifiSwitch"
        android:layout_marginTop="86dp"
        android:checked="false"
        android:switchPadding="100dp"
        android:textColor="#FFFFFF"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Credits"
        android:id="@+id/creditsButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="showCreditsAlert"/>

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wifi Hotspot on/off"
        android:id="@+id/wifiHotspotSwitch"
        android:textColor="#FFFFFF"
        android:layout_marginTop="34dp"
        android:switchPadding="45dp"
        android:checked="false"
        android:layout_below="@+id/settingsWifiSwitch"
        android:layout_alignLeft="@+id/settingsWifiSwitch"
        android:layout_alignStart="@+id/settingsWifiSwitch" />
</RelativeLayout>
